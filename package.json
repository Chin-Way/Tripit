{
  "name": "tripit",
  "version": "0.1.0",
  "private": true,
  "description": "TripIt — family trip planner. Survey-driven, nap-aware itineraries grounded in real venue data and personalized by Claude.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "qr": "node scripts/qr.mjs",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1",
    "@google/genai": "^2.8.0"
  },
  "comment": "AI SDKs (@anthropic-ai/sdk, @google/genai) are imported lazily only when the matching API key is set. The core server + rules engine run with no dependencies installed.",
  "devDependencies": {
    "qrcode": "^1.5.4"
  }
}
