node_modules/
.env
.env.local
.DS_Store
npm-debug.log*
*.log
.vercel

# local SQLite datastore (created at runtime; use Postgres in production)
data/*.db
data/*.db-wal
data/*.db-shm
tripit.db*

# exported feedback dataset (contains user content; curate before sharing)
feedback-export*.jsonl

# generated QR codes (run `npm run qr -- <your-url>` to make them)
public/qr.png
public/qr.svg
